<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Maria Eduarda | Estudante e Programadora</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;700&family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #6c5ce7;
            --primary-light: #a29bfe;
            --secondary-color: #4834d4;
            --accent-color: #fd79a8;
            --dark-color: #2d3436;
            --light-color: #f5f6fa;
            --text-color: #2d3436;
            --text-light: #636e72;
            --white: #ffffff;
            --black: #000000;
            
            --font-main: 'Montserrat', sans-serif;
            --font-secondary: 'Poppins', sans-serif;
            
            --transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            --shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            --shadow-hover: 0 20px 50px rgba(0, 0, 0, 0.15);
            --border-radius: 20px;
            --glass-effect: backdrop-filter: blur(12px) saturate(180%);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            scroll-padding-top: 100px;
        }

        body {
            font-family: var(--font-secondary);
            color: var(--text-color);
            background-color: var(--light-color);
            line-height: 1.7;
            overflow-x: hidden;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: var(--font-main);
            font-weight: 700;
            line-height: 1.3;
        }

        /* Efeito de gradiente animado mais suave */
        .gradient-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            background: linear-gradient(-45deg, #f5f7fa, #e8eaf7, #dfe3ff, #d5daff);
            background-size: 400% 400%;
            animation: gradient 20s ease infinite;
        }

        @keyframes gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Navbar flutuante mais elegante */
        .floating-nav {
            position: fixed;
            right: 40px;
            top: 50%;
            transform: translateY(-50%);
            z-index: 1000;
            background-color: rgba(255, 255, 255, 0.95);
            padding: 20px 12px;
            border-radius: 60px;
            box-shadow: var(--shadow);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .floating-nav ul {
            display: flex;
            flex-direction: column;
            gap: 25px;
        }

        .floating-nav li a {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-light);
            font-size: 1.2rem;
            transition: var(--transition);
            position: relative;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        }

        .floating-nav li a::before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            z-index: -1;
            transform: scale(0);
            transition: var(--transition);
            opacity: 0;
        }

        .floating-nav li a:hover, 
        .floating-nav li a.active {
            color: var(--white);
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(108, 92, 231, 0.3);
        }

        .floating-nav li a:hover::before,
        .floating-nav li a.active::before {
            transform: scale(1);
            opacity: 1;
        }

        /* Seção Hero mais moderna */
        .hero-section {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 120px 0;
            position: relative;
        }

        .hero-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 60px;
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 60px;
        }

        .hero-text {
            flex: 1;
            animation: fadeInLeft 1s ease;
        }

        .greeting {
            font-size: 1.3rem;
            color: var(--primary-color);
            margin-bottom: 15px;
            font-weight: 600;
            display: inline-block;
            background-color: rgba(108, 92, 231, 0.1);
            padding: 10px 20px;
            border-radius: 50px;
            letter-spacing: 1px;
        }

        .name {
            font-size: clamp(2.8rem, 6vw, 4.5rem);
            margin-bottom: 20px;
            color: var(--dark-color);
            line-height: 1.2;
            font-weight: 800;
        }

        .name span {
            color: var(--primary-color);
            position: relative;
            display: inline-block;
        }

        .name span::after {
            content: '';
            position: absolute;
            bottom: 8px;
            left: 0;
            width: 100%;
            height: 12px;
            background-color: rgba(108, 92, 231, 0.2);
            z-index: -1;
            border-radius: 8px;
        }

        .title {
            font-size: clamp(1.4rem, 3vw, 2rem);
            font-weight: 400;
            color: var(--text-light);
            margin-bottom: 40px;
            max-width: 650px;
            line-height: 1.6;
        }

        .hero-buttons {
            display: flex;
            gap: 25px;
            margin-top: 50px;
            flex-wrap: wrap;
        }

        .btn-main, .btn-secondary {
            padding: 18px 35px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 12px;
            position: relative;
            overflow: hidden;
            z-index: 1;
            letter-spacing: 0.5px;
        }

        .btn-main {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
            box-shadow: 0 15px 30px rgba(108, 92, 231, 0.3);
        }

        .btn-main:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(108, 92, 231, 0.4);
        }

        .btn-secondary {
            background-color: transparent;
            color: var(--primary-color);
            border: 2px solid var(--primary-color);
        }

        .btn-secondary:hover {
            background-color: var(--primary-color);
            color: var(--white);
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(108, 92, 231, 0.2);
        }

        /* Avatar mais moderno */
        .hero-visual {
            flex: 1;
            display: flex;
            justify-content: center;
            animation: fadeInRight 1s ease;
        }

        .avatar-container {
            position: relative;
            width: 400px;
            height: 400px;
        }

        .avatar-circle {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: linear-gradient(135deg, rgba(108, 92, 231, 0.1) 0%, rgba(162, 155, 254, 0.1) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
            box-shadow: 0 20px 50px rgba(108, 92, 231, 0.1);
        }

        .avatar-initials {
            font-size: 7rem;
            font-weight: 800;
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            z-index: 2;
        }

        .avatar-circle::before {
            content: '';
            position: absolute;
            width: 150%;
            height: 150%;
            background: conic-gradient(
                transparent 0deg,
                transparent 180deg,
                var(--primary-color) 180deg,
                var(--primary-color) 270deg,
                transparent 270deg,
                transparent 360deg
            );
            animation: rotate 8s linear infinite;
        }

        .avatar-circle::after {
            content: '';
            position: absolute;
            inset: 8px;
            border-radius: 50%;
            background-color: var(--light-color);
        }

        .avatar-decoration {
            position: absolute;
            width: 120%;
            height: 120%;
            top: -10%;
            left: -10%;
            border-radius: 50%;
            border: 20px solid rgba(108, 92, 231, 0.1);
            animation: pulse 4s infinite ease-in-out;
            z-index: -1;
        }

        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        /* Indicador de scroll mais bonito */
        .scroll-indicator {
            position: absolute;
            bottom: 60px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
        }

        .scroll-indicator span {
            display: block;
            width: 4px;
            height: 25px;
            background: linear-gradient(to bottom, var(--primary-color), var(--primary-light));
            border-radius: 10px;
            animation: bounce 2s infinite;
            transform-origin: center;
        }

        .scroll-indicator span:nth-child(1) { animation-delay: 0.1s; }
        .scroll-indicator span:nth-child(2) { animation-delay: 0.2s; height: 20px; }
        .scroll-indicator span:nth-child(3) { animation-delay: 0.3s; height: 15px; }

        .scroll-text {
            font-size: 0.9rem;
            color: var(--text-light);
            margin-top: 15px;
            opacity: 0.8;
            animation: fadeInOut 2s infinite;
            letter-spacing: 1px;
        }

        @keyframes fadeInOut {
            0%, 100% { opacity: 0.6; }
            50% { opacity: 1; }
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
        }

        /* Seções com efeito de vidro mais refinado */
        section {
            padding: 120px 0;
            position: relative;
        }

        .section-header {
            text-align: center;
            margin-bottom: 80px;
            position: relative;
        }

        .section-header h2 {
            font-size: clamp(2rem, 6vw, 3rem);
            margin-bottom: 20px;
            display: inline-block;
            position: relative;
        }

        .section-header h2 span {
            color: var(--primary-color);
            position: relative;
        }

        .section-header h2 span::after {
            content: '';
            position: absolute;
            bottom: 8px;
            left: 0;
            width: 100%;
            height: 10px;
            background-color: rgba(108, 92, 231, 0.2);
            z-index: -1;
            border-radius: 8px;
        }

        .section-divider {
            width: 100px;
            height: 5px;
            background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
            margin: 0 auto;
            border-radius: 3px;
            position: relative;
        }

        .section-divider::after {
            content: '';
            position: absolute;
            width: 40px;
            height: 12px;
            background-color: var(--accent-color);
            bottom: -4px;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 6px;
        }

        /* Seção Sobre mais elegante */
        .about-content {
            display: flex;
            gap: 60px;
            align-items: center;
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 60px;
        }

        .about-text {
            flex: 1;
            background-color: rgba(255, 255, 255, 0.9);
            padding: 50px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            transition: var(--transition);
        }

        .about-text:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow-hover);
        }

        .about-text h3 {
            font-size: 2rem;
            margin-bottom: 25px;
            color: var(--dark-color);
            position: relative;
            display: inline-block;
        }

        .about-text h3::after {
            content: '';
            position: absolute;
            bottom: 8px;
            left: 0;
            width: 100%;
            height: 10px;
            background-color: rgba(108, 92, 231, 0.2);
            z-index: -1;
            border-radius: 8px;
        }

        .about-text p {
            margin-bottom: 35px;
            color: var(--text-light);
            font-size: 1.2rem;
            line-height: 1.8;
        }

        .personal-info {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 25px;
            margin-top: 40px;
        }

        .info-item {
            display: flex;
            gap: 15px;
            align-items: center;
        }

        .info-label {
            font-weight: 600;
            color: var(--dark-color);
            min-width: 120px;
            font-size: 1.1rem;
        }

        .info-value {
            color: var(--text-light);
            background-color: rgba(108, 92, 231, 0.05);
            padding: 10px 20px;
            border-radius: 50px;
            flex: 1;
            font-size: 1.1rem;
        }

        .about-stats {
            flex: 1;
            display: grid;
            gap: 40px;
        }

        .stat-card {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 40px 30px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            transition: var(--transition);
            text-align: center;
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .stat-card:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-hover);
        }

        .stat-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, rgba(108, 92, 231, 0.1) 0%, rgba(162, 155, 254, 0.1) 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 25px;
            font-size: 2rem;
            color: var(--primary-color);
            border: 1px solid rgba(108, 92, 231, 0.2);
            transition: var(--transition);
        }

        .stat-card:hover .stat-icon {
            transform: rotate(15deg) scale(1.1);
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
        }

        .stat-card h3 {
            margin-bottom: 20px;
            font-size: 1.6rem;
            color: var(--dark-color);
        }

        .stat-card p {
            color: var(--text-light);
            font-size: 1.1rem;
            line-height: 1.7;
        }

        /* Seção Projetos mais bonita */
        .projects-section {
            background-color: rgba(108, 92, 231, 0.03);
        }

        .projects-container {
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 60px;
        }

        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(380px, 1fr));
            gap: 40px;
        }

        .project-card {
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            overflow: hidden;
            transition: var(--transition);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .project-card:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-hover);
        }

        .project-image {
            height: 220px;
            background: linear-gradient(135deg, rgba(108, 92, 231, 0.1) 0%, rgba(162, 155, 254, 0.1) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--primary-color);
            font-size: 4rem;
            transition: var(--transition);
        }

        .project-card:hover .project-image {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
        }

        .project-content {
            padding: 30px;
        }

        .project-content h3 {
            margin-bottom: 20px;
            color: var(--primary-color);
            font-size: 1.5rem;
            position: relative;
            display: inline-block;
        }

        .project-content h3::after {
            content: '';
            position: absolute;
            bottom: 5px;
            left: 0;
            width: 100%;
            height: 8px;
            background-color: rgba(108, 92, 231, 0.2);
            z-index: -1;
            border-radius: 5px;
        }

        .project-content p {
            color: var(--text-light);
            margin-bottom: 25px;
            font-size: 1.1rem;
            line-height: 1.7;
        }

        .project-link {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            color: var(--primary-color);
            font-weight: 600;
            font-size: 1.1rem;
            transition: var(--transition);
            padding: 8px 0;
            position: relative;
        }

        .project-link::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
            transition: var(--transition);
        }

        .project-link:hover {
            color: var(--secondary-color);
            transform: translateX(8px);
        }

        .project-link:hover::after {
            width: 100%;
        }

        /* Seção Contato mais elegante */
        .contact-section {
            background-color: rgba(108, 92, 231, 0.03);
        }

        .contact-container {
            display: flex;
            gap: 60px;
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 60px;
        }

        .contact-info {
            flex: 1;
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 35px;
        }

        .contact-card {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 40px 30px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            transition: var(--transition);
            text-align: center;
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .contact-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow-hover);
        }

        .contact-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, rgba(108, 92, 231, 0.1) 0%, rgba(162, 155, 254, 0.1) 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 25px;
            font-size: 2rem;
            color: var(--primary-color);
            border: 1px solid rgba(108, 92, 231, 0.2);
            transition: var(--transition);
        }

        .contact-card:hover .contact-icon {
            transform: scale(1.1);
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
        }

        .contact-card h3 {
            margin-bottom: 20px;
            font-size: 1.5rem;
            color: var(--dark-color);
        }

        .contact-card p {
            color: var(--text-light);
            font-size: 1.1rem;
        }

        .contact-form {
            flex: 1;
            background-color: rgba(255, 255, 255, 0.9);
            padding: 50px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            transition: var(--transition);
        }

        .contact-form:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-hover);
        }

        .form-group {
            margin-bottom: 30px;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 18px 25px;
            border: 1px solid rgba(0, 0, 0, 0.1);
            border-radius: var(--border-radius);
            font-family: var(--font-secondary);
            font-size: 1.1rem;
            transition: var(--transition);
            background-color: rgba(255, 255, 255, 0.7);
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.2);
            background-color: var(--white);
        }

        .btn-submit {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
            border: none;
            padding: 20px 35px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            cursor: pointer;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 12px;
            box-shadow: 0 15px 30px rgba(108, 92, 231, 0.3);
            width: 100%;
            justify-content: center;
            letter-spacing: 0.5px;
        }

        .btn-submit:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(108, 92, 231, 0.4);
            background: linear-gradient(135deg, var(--secondary-color), var(--primary-color));
        }

        /* Rodapé mais estiloso */
        .main-footer {
            background-color: var(--dark-color);
            color: var(--white);
            padding: 100px 0 0;
            position: relative;
        }

        .footer-content {
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 60px;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 60px;
        }

        .footer-logo {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 25px;
        }

        .footer-logo span {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            color: var(--white);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
            font-weight: 700;
            box-shadow: 0 10px 30px rgba(108, 92, 231, 0.3);
        }

        .footer-logo h3 {
            font-size: 1.8rem;
            font-weight: 700;
        }

        .footer-about p {
            color: rgba(255, 255, 255, 0.7);
            margin-bottom: 25px;
            line-height: 1.8;
            font-size: 1.1rem;
        }

        .footer-social {
            display: flex;
            gap: 20px;
            margin-top: 25px;
        }

        .footer-social a {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background-color: rgba(255, 255, 255, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.3rem;
            transition: var(--transition);
        }

        .footer-social a:hover {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(108, 92, 231, 0.3);
        }

        .footer-links h3 {
            font-size: 1.5rem;
            margin-bottom: 25px;
            position: relative;
            display: inline-block;
        }

        .footer-links h3::after {
            content: '';
            position: absolute;
            bottom: 5px;
            left: 0;
            width: 100%;
            height: 8px;
            background-color: rgba(162, 155, 254, 0.3);
            z-index: -1;
            border-radius: 5px;
        }

        .footer-links ul {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .footer-links a {
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 12px;
            color: rgba(255, 255, 255, 0.7);
            font-size: 1.1rem;
        }

        .footer-links a i {
            color: var(--primary-light);
            transition: var(--transition);
        }

        .footer-links a:hover {
            color: var(--white);
            transform: translateX(8px);
        }

        .footer-links a:hover i {
            color: var(--accent-color);
        }

        .footer-bottom {
            text-align: center;
            padding: 35px;
            margin-top: 80px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            font-size: 1rem;
            color: rgba(255, 255, 255, 0.5);
        }

        /* Animações */
        @keyframes fadeInLeft {
            from {
                opacity: 0;
                transform: translateX(-80px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes fadeInRight {
            from {
                opacity: 0;
                transform: translateX(80px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
                opacity: 1;
            }
            50% {
                transform: scale(1.05);
                opacity: 0.8;
            }
            100% {
                transform: scale(1);
                opacity: 1;
            }
        }

        /* Responsividade */
        @media (max-width: 1400px) {
            .hero-content,
            .about-content,
            .contact-container {
                padding: 0 50px;
            }
        }

        @media (max-width: 1200px) {
            .hero-content,
            .about-content,
            .contact-container,
            .projects-container {
                padding: 0 40px;
            }
            
            .footer-content {
                padding: 0 40px;
            }
        }

        @media (max-width: 992px) {
            .hero-content {
                flex-direction: column;
                text-align: center;
                gap: 80px;
            }
            
            .hero-buttons {
                justify-content: center;
            }
            
            .about-content {
                flex-direction: column;
                gap: 60px;
            }
            
            .contact-container {
                flex-direction: column;
                gap: 60px;
            }
            
            .footer-content {
                grid-template-columns: 1fr 1fr;
                gap: 50px;
            }
            
            .avatar-container {
                width: 350px;
                height: 350px;
            }
        }

        @media (max-width: 768px) {
            .name {
                font-size: 3.2rem;
            }
            
            .title {
                font-size: 1.6rem;
            }
            
            .personal-info {
                grid-template-columns: 1fr;
            }
            
            .projects-grid {
                grid-template-columns: 1fr;
            }
            
            .footer-content {
                grid-template-columns: 1fr;
                gap: 40px;
            }
            
            .floating-nav {
                right: 25px;
                padding: 15px 10px;
            }
            
            .floating-nav li a {
                width: 45px;
                height: 45px;
                font-size: 1.1rem;
            }
        }

        @media (max-width: 576px) {
            .hero-content,
            .about-content,
            .contact-container,
            .projects-container {
                padding: 0 30px;
            }
            
            .name {
                font-size: 2.8rem;
            }
            
            .title {
                font-size: 1.4rem;
            }
            
            .hero-buttons {
                flex-direction: column;
                gap: 20px;
            }
            
            .btn-main, .btn-secondary {
                width: 100%;
                justify-content: center;
            }
            
            .avatar-container {
                width: 280px;
                height: 280px;
            }
            
            .floating-nav {
                bottom: 25px;
                top: auto;
                right: 50%;
                transform: translateX(50%);
                flex-direction: row;
                padding: 12px 20px;
            }
            
            .floating-nav ul {
                flex-direction: row;
                gap: 20px;
            }
            
            .contact-info {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="gradient-bg"></div>
    
    <nav class="floating-nav">
        <ul>
            <li><a href="#home" class="nav-link active"><i class="fas fa-home"></i></a></li>
            <li><a href="#about" class="nav-link"><i class="fas fa-user"></i></a></li>
            <li><a href="#projects" class="nav-link"><i class="fas fa-project-diagram"></i></a></li>
            <li><a href="#contact" class="nav-link"><i class="fas fa-envelope"></i></a></li>
        </ul>
    </nav>

    <main>
        <section id="home" class="hero-section">
            <div class="hero-content">
                <div class="hero-text">
                    <h4 class="greeting">Olá, eu sou</h4>
                    <h1 class="name">Maria <span>Eduarda</span></h1>
                    <h2 class="title">Estudante e Entusiasta de Programação</h2>
                    <div class="hero-buttons">
                        <a href="#projects" class="btn-main"><i class="fas fa-project-diagram"></i> Meus Projetos</a>
                        <a href="#about" class="btn-secondary"><i class="fas fa-user"></i> Sobre Mim</a>
                    </div>
                </div>
                <div class="hero-visual">
                    <div class="avatar-container">
                        <div class="avatar-circle">
                            <div class="avatar-initials">ME</div>
                        </div>
                        <div class="avatar-decoration"></div>
                    </div>
                </div>
            </div>
            <div class="scroll-indicator">
                <span></span>
                <span></span>
                <span></span>
                <div class="scroll-text">Role para explorar</div>
            </div>
        </section>

        <section id="about" class="about-section">
            <div class="section-header">
                <h2>Sobre <span>Mim</span></h2>
                <div class="section-divider"></div>
            </div>
            <div class="about-content">
                <div class="about-text">
                    <h3>Quem sou eu?</h3>
                    <p>Sou uma estudante apaixonada por tecnologia e programação. Atualmente, além dos estudos acadêmicos, dedico meu tempo a desenvolver projetos pessoais que envolvem programação, explorando minha criatividade e habilidades técnicas.</p>
                    
                    <div class="personal-info">
                        <div class="info-item">
                            <span class="info-label">Nome:</span>
                            <span class="info-value">Maria Edu
